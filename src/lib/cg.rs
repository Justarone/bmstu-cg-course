use super::prelude::*;

#[allow(dead_code)]
#[allow(unused_variables)]
pub fn transform_and_flush(points_and_normals: &(Vec<Vec<Point3d>>, Vec<Vec<Point3d>>),
                           matrix: &Matrix4, pb: Pixbuf) {
    println!("transform_and_flush");
}
